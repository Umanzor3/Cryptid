package com.revature.cryptid.service;

import java.util.List;
import java.util.Optional;

import org.springframework.beans.factory.annotation.Autowired;

import com.revature.cryptid.model.User;
import com.revature.cryptid.repository.UserRepository;

public class UserService {
	
private UserRepository userRepository;
	
	@Autowired
	public void setCrytidRepository(UserRepository userRepository) {
		this.userRepository = userRepository;
	}
	
	public List<User> findAllUsers() {
		return userRepository.findAll();
	}
	
	public void deleteUserById(int id) {
		userRepository.deleteById(id);
	}
	
	public User getUserById(int id) {
		Optional<User> c = userRepository.findById(id);
		if (c.isPresent()) {
			return c.get();
		} else {
			return null;
		}
	}
	
	public User findCryptidByName(String name) {
		return userRepository.findUserByUsername(name);
	}
	
	public void createCryptid(Cryptids cryptid) {
		cryptidRepository.save(cryptid);
	}
	
	public int updateCryptid (Cryptids cryptid) {
		Cryptids c = cryptidRepository.save(cryptid);
		if (c != null) {
			return c.getId();
		}else {
			return 0;
		}
	}

}



